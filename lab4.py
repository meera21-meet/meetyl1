from turtle import Turtle
class ball(Turtle):
	def __init__(self,r,color,dx,dy):
		Turtle.__init__(self)
		self.r=r/10
		self.dx=dx
		self.dy=dy
		self.shape("circle")
		self.penup()
		self.color(color)

	def move__init__(screen_width,screen_height):
		current_x=current_x.xcor()
		current_y=current_y.ycor()
		new_x=
b=ball(3,"purple",6,8)	